backend = "glx";
vsync = true;

# No global opacity — let kitty control it
active-opacity = 1.0;
inactive-opacity = 1.0;

# Prevent terminal flicker
fade = false;
fade-exclude = [
  "class_g = 'kitty'"
];

# Disable shadows (Awesome handles borders well)
shadow = false;
